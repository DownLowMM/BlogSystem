<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.duan.blogos.service.dao.BloggerAccountDao">

    <select id="getAccountByName" resultType="com.duan.blogos.service.entity.BloggerAccount">
        SELECT
            a.id,
            a.username,
            a.register_date,
            a.password
        FROM blogger_account a
        WHERE a.username = #{bloggerName}
    </select>

    <select id="getAccountById" resultType="com.duan.blogos.service.entity.BloggerAccount">
        SELECT
            a.id,
            a.username,
            a.register_date,
            a.password
        FROM blogger_account a
        WHERE a.id = #{bloggerId}
    </select>

    <update id="update" parameterType="com.duan.blogos.service.entity.BloggerAccount">
        UPDATE blogger_account
        <set>
            <if test="username != null">
                username=#{username},
            </if>
            <if test="password != null">
                password=#{password},
            </if>
            <if test="registerDate != null">
                registerDate=#{registerDate},
            </if>
        </set>
        <where>
            id=#{id}
        </where>
    </update>

    <insert id="insert" parameterType="com.duan.blogos.service.entity.BloggerAccount" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO blogger_account (
        username,
        password,
        register_date
        ) VALUE (
        #{username},
        #{password},
        NOW()
        )
    </insert>

    <delete id="delete">
        DELETE FROM blogger_account
        WHERE id = #{value}
    </delete>

    <select id="listId" resultType="java.lang.Long">
        select id
        from blogger_account
        limit 0, #{value}
    </select>

</mapper>